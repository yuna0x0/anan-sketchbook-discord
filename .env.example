# Discord Bot Credentials
# Get it from https://discord.com/developers/applications
DISCORD_TOKEN=your_discord_bot_token
APPLICATION_ID=your_application_id

# Optional: Custom paths for data and assets
# DATA_DIR=/path/to/data
# ASSETS_DIR=/path/to/assets

# Optional: Discord Analytics token (https://discordanalytics.xyz)
# DISCORD_ANALYTICS_TOKEN=your_discord_analytics_token

# Optional: override Discord Analytics API URL (for self-hosted instances)
# DISCORD_ANALYTICS_API_URL=https://discordanalytics.example.com

# Node.js environment flag honored by many libraries (Discord Analytics
# only sends stats when this is "production"; other packages also enable
# production optimizations and quieter error output).
# Set this on your production deploy; leave commented out for local dev.
# NODE_ENV=production

# Optional: tuning for the in-memory Adjust/Effects editing sessions.
# Sessions hold generation parameters (and any uploaded image) so the
# Adjust/Effects buttons can re-render a message; they are never persisted.
# ADJUST_SESSION_TTL_MINUTES=1440
# ADJUST_SESSION_MAX_ENTRIES=200
# ADJUST_SESSION_MAX_MB=64

# Optional: limits on user-uploaded images (DoS protection).
# MAX_IMAGE_UPLOAD_MB=8
# MAX_IMAGE_PIXELS_MP=16
